CFLAGS = @CFLAGS@ -I./include -I../CommonLib/include
.c.o:
	@CC@ $(CFLAGS) $< -o $@

LIB = libAppli

SOURCES = \
	./ErrorMgr/ErrorMgr.c \
	./FileMgr/FileMgr.c \
	./OptionMgr/OptionMgr.c \
	./TimeMgr/TimeMgr.c \
	./SdbMgr/SdbMgr.c

HEADERS = $(SOURCES:.c=.h)

OBJECTS = $(SOURCES:.c=.o)

all: libAppli.a

libAppli.a: includes $(OBJECTS)

	@MAKELIB@ $(OBJECTS)
	@RANLIB@ $@

includes:
	mkdir -p include
	for f in $(HEADERS); do \
	    ln -sf .$$f -t include; \
	done
	
clean:
	@CLEAN@ include $(OBJECTS) libAppli.a

distclean:
	@CLEAN@ Makefile
